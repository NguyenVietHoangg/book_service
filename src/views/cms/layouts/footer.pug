footer.main-footer
  .container
    .pull-right.hidden-xs
      b Version
      |  2.4.0
    strong
      | Copyright © 2019-2020 
      a(href='/') ndhung.dev@gmail.com
      | .
    |  All rights
    |         reserved.
    script. 
        let xhttp = new XMLHttpRequest();
        xhttp.onreadystatechange = function () {
          if (this.readyState == 4 && this.status == 200) {
            //- window.location.href = urlManager;
          }
        };

        function deleteRow(url){
          Swal.fire({
            title: 'Xóa dữ liệu?',
            text: "Bạn có chắc muốn xóa dữ liệu đã chọn",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Xóa'
          }).then((result) => {
            if (result.value) {
              xhttp.open('DELETE', url, true);
              xhttp.send();
              location.reload();
            }
          })
        }

        function updateForm(form,id){
          if(id){
            Swal.fire({
              title: 'Cập nhật dữ liệu?',
              text: "Bạn có chắc muốn cập nhật liệu đã chọn",
              icon: 'warning',
              showCancelButton: true,
              confirmButtonColor: '#3085d6',
              cancelButtonColor: '#d33',
              confirmButtonText: 'Cập nhật'
            }).then((result) => {
              if (result.value) {
                form.submit()
              }
            })
          }
          else{
            form.submit()
          }
        }